# import "../fragments/PayrollFragment.graphql"

fragment PayrollDetailFragment on payrolls {
  ...PayrollFragment
  client {
    id
    name
  }
  payroll_cycle {
    id
    name
  }
  payroll_date_type {
    id
    name
  }
}